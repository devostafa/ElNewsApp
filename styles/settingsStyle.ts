import { StyleSheet } from "react-native";

export const settingsStyle = StyleSheet.create({
  settingsPageDesktop: {
    flex: 1,
    flexDirection: "column",
    gap: 10,
  },
  section: {
    flex: 1,
    flexDirection: "column",
    gap: 5,
  },
});
